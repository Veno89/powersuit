# Validation reports

Reports in this folder are canonical JSON evidence produced by
`validate_and_generate_lods.py`. A `FAIL` report is useful evidence, not a
release approval: it records exactly why an input is still a review maquette.

Generated `.blend` derivatives belong in `../derivatives/` and remain ignored.
